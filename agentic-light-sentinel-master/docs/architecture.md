# Architecture

- **Next.js App Router (TS)** for UI + API routes
- **Leaflet** maps (React-Leaflet)
- **Postgres + Prisma** for regions + yearly metrics
- **Agentic loop**: scripts/ingest-viirs → process-hotspots → notify
- **Feedback**: /api/learn captures field intel

